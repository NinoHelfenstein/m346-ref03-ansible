- host: all
  - name: Install Maeven
    ansible.builtin.apt:
      name: maven, docker
      state: present
      
  - name: Clone Porject
    ansible.builtin.git:
    repo: https://gitlab.com/bbwrl/m346-ref-card-03.git
    dest: /ref03

  - name: Build Ref03
    ansible.builtin.command:
      cmd: mvn clean install
    args:
      chdir: /ref03

  - name: Start Ref03
    ansible.builtin.command:
      cmd: java -DB_USERNAME="jokedbuser" -DB_PASSWORD="123456" -jar  target/architecture-refcard-03-0.0.1-SNAPSHOT.jar &
    args:
      /ref03
